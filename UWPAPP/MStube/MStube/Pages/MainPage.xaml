<Page
    x:Class="MStube.MainPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MStube"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid x:Name="OuterContainer" Padding="12,12,12,12" Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <ListView
            x:Name="VideoBriefList"
            HorizontalAlignment="Left"
            Margin="10,30,10,10"
            VerticalAlignment="Top"
            IsItemClickEnabled="True" 
            ItemClick="ItemClicked">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid
                        MaxHeight="150">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="16" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <Image 
                            Grid.Row="0" 
                            Grid.RowSpan="3"
                            Grid.Column="0"
                            Width="120"
                            Height="120" 
                            HorizontalAlignment="Left" 
                            VerticalAlignment="Center" 
                            Source="{Binding ImageSourceUri}" 
                            Stretch="UniformToFill"/>
                        <TextBlock 
                            x:Name="titleText"
                            Grid.Row="0"
                            Grid.Column="2"
                            Style="{StaticResource TitleTextBlockStyle}"
                            Text="{Binding Title}"/>
                        <TextBlock 
                            Grid.Row="2"
                            Grid.Column="2"
                            Margin="0,4,0,0"
                            Text="{Binding Description}" 
                            HorizontalAlignment="Left"
                            VerticalAlignment="Top" 
                            Style="{StaticResource BodyTextBlockStyle}"/>
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                </Style>
            </ListView.ItemContainerStyle>
        </ListView>
    </Grid>
</Page>
