<Page
    x:Class="MStube.VideoPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:MStube"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid x:Name="VideoDetail">
            <TextBlock x:Name="textTitle" HorizontalAlignment="Center" Height="33" Margin="10,10,0,0" TextWrapping="Wrap" Text="{Binding VideoDetail.VideoTitle}" VerticalAlignment="Top" FontSize="21.333" Width="Auto"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="80*"></ColumnDefinition>
                </Grid.ColumnDefinitions>
                <WebView x:Name="videoView" HorizontalAlignment="Center" Margin="10,46,10,0" Width="300" Height="200" VerticalAlignment="Top"/>
            </Grid>
            <TextBlock x:Name="textDescription" HorizontalAlignment="Left" Margin="10,303,0,0" TextWrapping="Wrap" Text="{Binding VideoDetail.VideoDescription}" VerticalAlignment="Top" Height="327" Width="80"/>
            <Button x:Name="backButton" Content="Back" HorizontalAlignment="Right" Height="96" Margin="0,0,10,10" VerticalAlignment="Bottom" Width="124" Click="backButton_Click"/>
        </Grid>
    </Grid>
</Page>
